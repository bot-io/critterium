/**
 * Critterium — Render Module
 *
 * PixiJS v8 adapter for rendering the simulation.
 * Batched tinted sprites, per-type texture swap,
 * per-particle rotation from velocity heading.
 */

export {};
